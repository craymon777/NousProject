<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nous">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nous">
        <activity android:name=".PostActivity"></activity>
        <activity android:name=".HistorySkillActivity" />
        <activity android:name=".ScienceSkillActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".SkillCompletedActivity" />
        <activity android:name=".Animal1Stage1Activity" />
        <activity android:name=".LearningAnimalActivity" />
        <activity android:name=".AnimalSkillsActivity" />
        <activity android:name=".RankingActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".BadgesActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ForgotPasswordActivity"></activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".EditProfileActivity"></activity>

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>